# 历史现状与迁移

> 跨主题请从 [[README]] 进入；发现矛盾时以 [[1-规划评审]] 为准。

> 日期：2026-07-27（v0.8 修订：**Hermes 退场，只做 OpenClaw**；新增**执行调度层**（[[4-执行调度与AI]]）；语言决策重开——建议转 TypeScript（[[5-技术架构与选型]]））
> 性质：产品设计 + 会话交接，供下一场对话 / 本人续做
> 状态：v0.1 MVP 已脚手架（Python + Hermes）；**v0.8 起该脚手架的宿主适配部分作废，core 逻辑仍可参考**
>
> ⚠️ **本版有三个待拍板决策，全部阻塞开工，集中列在 [[11-交接与待决]]。**
>
> **2026-07-30 补注：** 本文记录 v0.8 当时的迁移判断。执行调度的现行决定已改为
> `Taskfold ACP Runner -> OpenClaw 公共 ACP Runtime -> @openclaw/acpx -> Claude Code / Codex / OpenCode`；
> 下文有关 CLI backend 的内容仅保留为历史调研和 fallback 背景，不能作为 M4 实现依据。

---

## 修订记录

| 版本 | 变更 |
|------|------|
| v0.1 | 增强版个人看板；单一 card 模型；仅 Hermes |
| v0.2 | 定位升级为 AI-native 敏捷协作平台；产品/发布/迭代/工作项模型；Hermes + OpenClaw 双适配 |
| v0.3 | 补齐**流程管理 / 工作项管理 / 工作规划 / 工作计划管理**四大模块（[[3-需求与工作管理]]）；新增**技术栈决策**（[[5-技术架构与选型]]），含开源项目选型调研 |
| v0.4 | **形态反转**：终局是独立软件，Hermes / OpenClaw 降级为分发渠道（[[2-产品定位与范围]]）；新增**独立软件架构预留**（[[5-技术架构与选型]]）与 **License / 知识产权**（[[5-技术架构与选型]]）；数据层补身份模型与可移植性（[[7-数据与接口]]） |
| v0.5 | 宿主扩展到 VS Code / Codex 等（[[6-OpenClaw集成]]）；**集成契约升级为 localhost HTTP + MCP**（[[5-技术架构与选型]]、[[4-执行调度与AI]]）；新增**参考代码的合法边界**（[[5-技术架构与选型]]） |
| **v0.6** | **定位反转：GSD 流程是核心，看板是载体**（[[2-产品定位与范围]]、[[3-需求与工作管理]]）；**gsd-core 内化——vendor 其 TS 代码入本仓库，不做外部依赖**（[[5-技术架构与选型]]）；**数据真相按实体分权**：GSD 产物在文件、taskfold 独有实体在 SQLite（[[7-数据与接口]]）；文件布局改用 `.planning/` 兼容（[[7-数据与接口]]）；全开源 + MIT 定案，商业化相关复杂度移除（[[5-技术架构与选型]]） |
| **v0.7** | **OpenClaw 能力映射补齐**（[[6-OpenClaw集成]]，据官方文档，未实测）：能注册全页 tab，但**只支持 JS/TS、UI 走 sandboxed frame、in-process 无沙箱**；新增 **[[6-OpenClaw集成]] 前端策略**——`dist/standalone/` 服务 3/4 宿主，Hermes IIFE 是特例；看板 UI 抄源定案（[[5-技术架构与选型]]）与前端构建链延后（[[5-技术架构与选型]]） |
| **v0.8** | **宿主收敛：Hermes 退场，当前只做 OpenClaw 插件**（[[2-产品定位与范围]]、[[6-OpenClaw集成]]）。连带三处推翻：①[[5-技术架构与选型]]「抄 Hermes 官方 kanban 插件」**作废**（IIFE 绑死宿主 SDK）；②[[6-OpenClaw集成]] 路线甲**作废**，standalone 成唯一路线；③**[[5-技术架构与选型]] Python 结论失去唯一依据，语言决策重开**——建议转 TypeScript。<br>**新增执行调度层**（[[4-执行调度与AI]]）：taskfold 是调度层不是执行者，驱动 Claude CLI / codex / opencode 干活；查明 **OpenClaw 已内建 CLI backend 机制**（`api.registerCliBackend`，捆绑 `claude-cli`，自带 session 持久化 + MCP bridge + skills 透传），执行层大半可复用生态。<br>新增 **[[11-交接与待决]] 待决清单** |

v0.1 的**核心逻辑**（`db.py` 的表结构与状态机）不推翻，仍作为设计起点；但 **Hermes 适配层（`dashboard/`）与 Python 实现本身在 v0.8 起是待议项**，见 [[5-技术架构与选型]] 与 [[7-数据与接口]]。

---

## 本机现状与路径

### 已完成（v0.1，⚠️ v0.8 起部分作废）

| # | 事项 | v0.8 状态 |
|---|------|----------|
| 1 | 选型：Hermes 自定义 Dashboard 插件 | ❌ **作废**（[[2-产品定位与范围]]） |
| 2 | 命名 taskfold；数据 MD + SQLite | ✅ 保留（MD 布局改 `.planning/`，[[7-数据与接口]]） |
| 3 | 脚手架写入 `/home/john/src/personal/Taskfold` | ⚠️ 后端约 400 行 Python，**是否重写取决于 [[11-交接与待决]] 的决策 A** |
| 4 | `scripts/install.sh` 已执行，`~/.hermes/plugins/taskfold` symlink 已建 | ❌ **可删**（Hermes 退场后无意义） |
| 5 | 冒烟通过：建卡 `g_35af990213`，MD 已落盘 | ✅ 验证了逻辑；数据本身可丢（[[7-数据与接口]]） |
| 6 | `git init` 已做，**尚未 commit / 尚未推 GitHub** | ⚠️ 仍未提交，有丢失风险（M1 第 8 项） |

**仍有价值的部分**：`db.py` 的表结构、`STATUSES` / `PRIORITIES` 状态机、`_now()` 的 ISO-8601 UTC 约定、`g_` + uuid 短 id 方案 —— 这些是设计资产，与语言无关，转 TS 时照搬。

### 已知待清理的宿主耦合

> **若 [[11-交接与待决]] 的决策 A 决定转 TS，本表在重写中自然消失，不需要单独清理动作。** 留在这里是因为它们记录了「哪些设计不能再犯」。

| 位置 | 问题 | 转 TS 后 |
|------|------|---------|
| `taskfold/config.py:9` | `hermes_home()` 读 `HERMES_HOME` —— core 不该知道宿主 | 不重现 |
| `taskfold/config.py:14` | `state_dir()` 落在 `~/.hermes/taskfold` —— 应为 `~/.taskfold` | 不重现 |
| `taskfold/config.py:24` | `DEFAULT_MD_ROOT` 硬编码 `/home/john/桌面/Work/taskfold` | 不重现 |
| `taskfold/db.py` | 无 `Database` 抽象，`sqlite3` 直连（[[5-技术架构与选型]]） | 重写时按接口做 |
| `taskfold/db.py:158` | `md_store` 直接调用，无 `BodyStore` 抽象（[[5-技术架构与选型]]） | 同上 |
| `dashboard/` 目录 | Hermes 适配层 | **整体删除** |

### 路径一览（v0.8 更新）

| 用途 | 路径 | 状态 |
|------|------|------|
| 代码仓 | `/home/john/src/personal/Taskfold` | ✅ |
| ~~Hermes 插件链接~~ | ~~`~/.hermes/plugins/taskfold`~~ | ❌ 可删 |
| SQLite（旧） | `~/.hermes/taskfold/taskfold.db` | ⚠️ 应迁至 `~/.taskfold/` |
| MD 根（v0.1 遗留） | `/home/john/桌面/Work/taskfold/` | ❌ 作废，改用各项目 `.planning/` |
| 本文档 | `/home/john/桌面/Work/personal/taskfold-交界文档.md` | ✅ |
| OpenClaw | **本机未安装** —— M1 第 0 步要装 | ⬜ |

环境变量：`TASKFOLD_HOME`（目标）；现存 `GSDBORD_MD_ROOT` / `GSDBORD_DB_PATH` / `HERMES_HOME`（后者应移除）

### 立刻要做的（v0.8 替换原 Hermes 验收）

```bash
# 1. 装 OpenClaw，测 [[6-OpenClaw集成]] 的四项（M1 第 0 步）
#    重点：nativeToolMode always-on 下 Read/Edit/Bash 是否完整
#         frame 能否连 127.0.0.1 + WebSocket
#         插件能否程序化起会话
#         claude --resume 能否接管 -p --session-id 的会话

# 2. 拍板 [[11-交接与待决]] 的三个决策（A 最优先）

# 3. 先 commit 一次现有代码（无论后面是否重写，先保住）
```

> ⚠️ 原「重启 hermes dashboard 验收 taskfold Tab」已无意义 —— 那条路不走了。**这也意味着 v0.1 那份脚手架的 UI 从头到尾没被人眼看过一次，就已经退役了。** 记录下来当个教训：那 400 行是在「宿主决定语言」这个后来被推翻的前提上写的。

---
