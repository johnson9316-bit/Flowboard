# 数据、接口与度量

> 跨主题请从 [[README]] 进入；发现矛盾时以 [[1-规划评审]] 为准。

「科学度量」要落到具体指标，否则是空话。指标由 `events`、`run_events` 与文件投影计算；`metric_snapshots` 只保存每日时间序列，不维护另一套业务真相。

| 指标 | 定义 | 用途 |
|------|------|------|
| **燃尽图** | 当前重点剩余点数 / 天 | 当前计划健康度 |
| **累积流图 CFD** | 各状态工作项数 / 天 | 识别瓶颈环节 |
| **周期时间 Cycle Time** | `doing` → `done` 耗时 | 交付效率 |
| **前置时间 Lead Time** | 创建 → `done` 耗时 | 端到端响应速度 |
| **吞吐量 Throughput** | 滚动 14 天完成项数 | 个人交付节奏 |
| **速率 Velocity** | 滚动 14 天完成点数 | 当前重点规划的参考，不作为团队容量承诺 |
| **缺陷密度** | bug 数 / 完成故事点 | 质量 |
| **缺陷逃逸率** | 上线后发现 bug / 总 bug | 测试有效性 |
| **阻塞时长** | `blocked` 累计停留 | 个人执行卡点 |
| **需求交付周期** | `inbox` → `done` | 需求管理全景 |
| **范围变更率** | 当前重点中途增删项 / 初始重点项 | 计划质量（[[3-需求与工作管理]]） |

**GSD 专属指标（v0.6 新增，其他看板给不出的）**：

| 指标 | 定义 | 用途 |
|------|------|------|
| **阶段停留分布** | 各 GSD 阶段耗时占比 | 看瓶颈在 Discuss（想不清）还是 Execute（做不完） |
| **Verify 返工率** | Verify → Execute 回环次数 / phase 数 | **最有价值的一个**：直接反映 Plan 质量 |
| **Plan 通过率** | plan-checker 一次通过 / 总计划数 | 规划成熟度 |
| **Wave 并行度** | 平均每 wave 的 plan 数 | 拆解粒度是否合理；并行度低说明依赖过密 |
| **Phase 交付周期** | Discuss 起 → Ship 止 | GSD 循环的端到端速度 |
| **上下文重置次数** | 每 phase 的 session 边界数 | 是否频繁被 context rot 打断 |
| **人机操作比** | `events.actor_type` 中 agent 与 human 的比例 | AI 实际承担了多少（[[7-数据与接口]]） |

**执行调度指标（v0.8 新增，来自 `runs` / `run_events`，[[4-执行调度与AI]]）**：

| 指标 | 定义 | 用途 |
|------|------|------|
| **一次成功率** | `done` 的 run / 总 run | 上下文包（[[4-执行调度与AI]]）组装得好不好 |
| **人工介入率** | 进过 `awaiting_human` 的 run / 总 run | **调度层的核心 KPI**：目标是让它降下来，但不是降到 0 |
| **平均等人时长** | `awaiting_human` → 人响应的耗时 | 人是不是瓶颈；巡检（[[4-执行调度与AI]]）该多久跑一次 |
| **介入原因分布** | 按 [[4-执行调度与AI]] 的触发条件分类 | 定位改进点：是提问多、还是权限卡、还是 Verify 老失败 |
| **会话成本** | token / 时长，按 phase 与 work_item 汇总 | 预算与 ROI |
| **中止率** | `aborted` / 总 run | 跑飞的频率 |
| **并发实际值** | 同时 running 的 run 数分布 | `max_concurrent_runs` 该调大还是调小（[[4-执行调度与AI]]） |

> 这些指标的原始数据来自 `.planning/`、`STATE.md` 与 `runs` 表，但**时间序列必须由 flowboard 自己按天快照**——`.planning/` 只存当前状态（[[7-数据与接口]] 铁律 3）。

> 度量红线：**不做人效排名、不按人统计产出**。指标用于改进流程，不用于考核个人。

---

## 数据设计

### 数据真相（⚠️ v0.6 **调整**：按实体分权，不再一刀切）

**旧决策（v0.1–v0.5）：SQLite 是唯一流程真相，MD 只是正文。** GSD 内化后这条不够用了——GSD 产物是 agent 要读、Git 要版本化的文件，不能降级为「SQLite 的导出」。

**但也不必反过来一刀切。** gsd-core 已 vendor 进本仓库（[[5-技术架构与选型]]），但仍需假设用户或官方工具会改动文件，因此必须**按实体划分真相归属**：

| 实体 | 真相在哪 | 理由 |
|------|---------|------|
| **GSD 产物**：`PROJECT.md`、`REQUIREMENTS.md`、`ROADMAP.md`、`STATE.md`、`phases/**` | **文件**（`.planning/` 兼容布局） | agent 要直接读；Git 要版本化与合并；这是 v0.1 就定的差异化 |
| **flowboard 独有**：个人当前重点、bug、RICE、links、执行者标识、**度量历史** | **SQLite** | 关系型、需组合查询；**GSD 根本没有这些的文件格式**，硬塞进 MD 是自找麻烦 |
| **执行账本**：`runs`、`run_events` | **SQLite** | 跨项目才能算的东西；**transcript 全文不存**，只存 `host_ref_kind + host_ref_id` 与可选可恢复会话指针（[[4-执行调度与AI]]、[[14-OpenClaw技能策略]]） |
| **复盘档**（v0.8）：`TIMELINE.md`、`HANDOFF.md` | **文件**（phase 目录下 `runs/`） | 要跟着 phase 一起 Git 版本化、要能被 agent 读；但**不改上游既有文件格式**（[[4-执行调度与AI]]，放置位置待实测 [[6-OpenClaw集成]]） |

**铁律**：

1. **保持 `.planning/` 格式兼容**，即便逻辑已内化。收益：用户既有的 GSD 项目可直接被读取（天然的采纳入口），且与官方 gsd-core 双向互操作。
2. **写 GSD 产物一律经 vendored 的 `gsd-tools.cjs`**，不自己拼 MD —— 免费获得其 `STATE.md.lock` 原子写语义。
3. **文件侧的投影必须可重建** —— `flowboard reindex` 能从 `.planning/` 重算 SQLite 中所有 GSD 派生列。
4. **SQLite 中的独有实体是原生数据，不可重建，须备份**（尤其 `metric_snapshots`：`.planning/` 只存当前状态，不存时间序列，CFD / 燃尽全靠按天快照）。
5. Git 跟 `.planning/` 与代码，**不跟 SQLite**（`.gitignore` 加 `flowboard.db*`）。
6. **仍要假设文件会被外部改动** —— 用户可能同时装着官方 gsd-core，或手工编辑 MD。需变更检测（[[5-技术架构与选型]]）。

> 这条调整现在改很便宜（SQLite 里还没有真实数据），堆了数据再改会痛。**M2 落地前必须完成。**

### 表结构演进目标

```sql
-- 已有（v0.1）
boards, cards, events

-- 新增 / 改造
products     (id, name, key, description, created_at, archived)
releases     (id, product_id, name, start_date, end_date, goal, status)
work_items   (id, product_id, type, title, status, priority,
              labels, points, position, md_path,
              rice_reach, rice_impact, rice_confidence, rice_effort,
              extra_json, created_at, updated_at, archived)
focus_items  (work_item_id, position, added_at, removed_at)
              -- 当前重点列表；历史变化由 events 留痕
links        (from_id, to_id, kind, created_at)
comments     (id, item_id, actor_type, actor_id, body, created_at)
              -- 评论原生存 SQLite，不写入 .planning/
events       (id, item_id, kind, from_value, to_value, actor_type, actor_id, created_at)
work_items_fts                                            -- FTS5 虚表，全文检索

-- 执行调度（v0.8 新增，[[4-执行调度与AI]]）
projects     (id, name, repo_path, planning_path, watched, created_at)
             -- watched = 是否纳入每日巡检（[[4-执行调度与AI]]）
runs         (id, work_item_id, project_id, phase_key, plan_key,
              -- phase_key / plan_key 的映射规则须在 M2 前按 [[1-规划评审]] 定义
              backend,              -- 'openclaw-coding-agent' / 'sdk' / …
              host_ref_kind,        -- 'task' / 'session' / 'run'；外部执行引用的类型
              host_ref_id,          -- ★ 对应的任务、会话或 run ID
              resumable_session_id, -- 可空；仅支持真终端接管的后端提供
              status,               -- queued|running|awaiting_human|done|failed|aborted
              awaiting_reason,      -- [[4-执行调度与AI]] 的触发分类
              started_at, ended_at, tokens_in, tokens_out, cost, exit_summary)
run_events   (id, run_id, kind, summary, created_at)
             -- ★ 只存里程碑摘要，不存 transcript 全文（[[4-执行调度与AI]]）
metric_snapshots (date, scope, kind, payload_json)
             -- ★ 原生数据，不可重建，须备份（[[7-数据与接口]] 铁律 4）
```

`cards` → `work_items` 为改名 + 加列，v0.1 数据可原地迁移。

> **`runs.host_ref_kind + host_ref_id` 是执行账本的外部关联键**：它用于取消、观察和调阅宿主任务。`resumable_session_id` 只在后端确认可接管时写入；`takeover` API 必须据此返回命令或明确返回“该 run 不支持终端接管”。

`events` 扩展 `from_value / to_value / actor_type / actor_id` —— **[[7-数据与接口]] 度量全靠它，这是最优先的 schema 改动**。

工作流定义、自动化规则不入库，走 YAML 文件（人可读、可 Git 版本化）。

### 文件布局（**采用 `.planning/` 兼容布局**）

不再自建一套 MD 目录，直接沿用 GSD 的既定结构（每个项目仓库一份）：

```
<项目仓库>/.planning/
├── PROJECT.md              # 愿景、约束、决策       → Product
├── REQUIREMENTS.md         # v1 / v2 / out-of-scope → 需求分级
├── ROADMAP.md              # phase 分解 + 状态      → 路线图（[[3-需求与工作管理]]）
├── STATE.md                # 位置 / 决策 / 阻塞 / 度量（+ STATE.md.lock）
├── config.json             # 工作流配置
├── continue-here.md        # /gsd-pause-work 的会话恢复上下文
└── phases/
    └── XX-phase-name/
        ├── XX-CONTEXT.md       # Discuss 产物
        ├── XX-UI-SPEC.md       # UI Design 产物（条件）
        ├── XX-RESEARCH.md      # Plan 产物
        ├── XX-YY-PLAN.md       # 执行计划
        ├── XX-YY-SUMMARY.md    # 执行结果
        ├── XX-VERIFICATION.md  # Verify 产物
        └── XX-UAT.md           # 用户验收
```

**flowboard 自己的文件**（不混入 `.planning/`，避免污染上游格式）：

```
<FLOWBOARD_HOME>/                 # M2+ 的 flowboard 插件数据目录；M1 完全沿用复制的 Workboard 存储约定
├── flowboard.db              # SQLite：独有实体 + 会话账本 + 派生索引
├── flowboard.db.bak-<时间戳>  # reindex 前自动备份（[[7-数据与接口]]）
├── workspaces.json         # 要聚合哪些项目仓库（[[5-技术架构与选型]]）
├── workflows.yaml          # story / task / bug 的流程定义（[[3-需求与工作管理]]）
├── automations.yaml        # 自动化规则（[[4-执行调度与AI]]）
├── orchestrator.yaml       # ★ v0.8：max_concurrent_runs、repo 互斥策略、预算阈值、
│                           #   默认 backend、permission-mode（[[4-执行调度与AI]]）
└── patrol/                 # ★ v0.8：巡检报告历史（[[4-执行调度与AI]]）
```

> 分界：**`.planning/` 归 GSD 格式，`<FLOWBOARD_HOME>/` 归 flowboard**。不往对方目录里写东西，互操作性才保得住。
>
> ⚠️ **v0.8 唯一的例外**：复盘档写在 `.planning/phases/XX-*/runs/`（[[4-执行调度与AI]]）。理由与边界：**不改上游既有文件的格式，只新增一个上游没有的子目录**，为的是让复盘跟着 phase 一起 Git 版本化。这个例外的合法性依赖 [[6-OpenClaw集成]] 实测（gsd-core 归档是否清理未知目录）；若不成立就退回 `<FLOWBOARD_HOME>/runs/`。

### 迁移策略

1. **v0.1 遗留数据**：`cards/*.md` 与 `cards` 表是 MVP 冒烟产物（仅 1 张卡），**直接丢弃即可**，不值得写迁移器
2. **接入已有 GSD 项目**：扫描配置的 workspace 根目录，发现 `.planning/` 即注册为 Product，`flowboard reindex` 建索引
3. **无 GSD 的新项目**：调 vendored 的 `/gsd-new-project` 脚手架生成 `.planning/`
4. 任何重建索引前自动备份 `flowboard.db` → `flowboard.db.bak-<时间戳>`
5. 提供 `flowboard reindex --dry-run` 与 `flowboard doctor`（校验文件与索引一致性）

**v0.8 补充：代码层面的「迁移」**（若 [[11-交接与待决]] 的决策 A 决定转 TS）

| 资产 | 处理 |
|------|------|
| `db.py` 的表结构、`STATUSES` / `PRIORITIES`、`_now()` 的 ISO-8601 UTC 约定、`g_`+uuid 短 id | ✅ **照搬**，与语言无关 |
| `db.py` / `md_store.py` / `config.py` 的实现 | 重写（~400 行，最机械的 CRUD） |
| `dashboard/`（Hermes 适配层） | ❌ **整体删除** |
| `scripts/install.sh`（建 Hermes symlink） | ❌ 删除，改为 npm 包安装 |
| 旧 SQLite `~/.hermes/flowboard/flowboard.db` | 丢弃（只有 1 张冒烟卡） |
| 旧 MD 根 `/home/john/桌面/Work/flowboard/` | 丢弃 |

> 因为库里只有 1 张测试卡，**这次「迁移」实际上是「重来」**，几乎零数据成本。这是现在拍板 [[11-交接与待决]] 的决策 A 最便宜的时机——堆了真实数据再转语言就得写迁移器了。

### 执行者标识与可移植性

**执行者标识**——个人版不建立 `users`、`assignee`、角色或权限系统，但仍要区分人、agent 与系统，才能解释事件、复盘 run 与计算人机操作比。

| 项 | 做法 |
|----|------|
| `events` / `comments` | 使用 `actor_type + actor_id`，不存自由字符串 |
| 人 | 固定标识 `human:local-owner`；M1 仅支持本机所有者 |
| agent | `agent:<backend>:<name>`，例如 `agent:openclaw:claude-cli` |
| 系统 | `system:flowboard`，用于 reindex、自动化和巡检 |
| 多人扩展 | 不预留用户表和角色。若重新立项小团队版，先设计认证、同步、权限与数据迁移，再引入 `users` |

**可移植性**——当前版本是单机个人工具，不为未承诺的团队部署引入 Postgres、权限或同步复杂度。`Database` 抽象只负责隔离 SQLite 驱动与便于测试；当小团队方向重新立项时，再以实际并发和部署需求评估数据库迁移。

---

## API 设计

**M2+：HTTP 路由与数据接口遵循 OpenClaw 插件 API 及复制的 Workboard 路由约定；M1 不新增独立 API。**

| Method | Path | 模块 |
|--------|------|------|
| GET | `/health` | — |
| GET/PUT | `/config` | — |
| GET/POST | `/products` | ② |
| GET/POST | `/releases` | ③ |
| GET | `/roadmap` | ③ 路线图视图数据 |
| GET/PUT | `/focus` | ④ 当前重点的读取、排序与增删 |
| GET | `/metrics/:kind` | ⑤ 当前重点燃尽、吞吐量、周期时间等 |
| GET/POST | `/items` | ② 列表（支持筛选）/ 创建 |
| GET/PATCH/DELETE | `/items/:id` | ② |
| POST | `/items/:id/transition` | ① **走流程校验的状态流转** |
| POST | `/items/:id/links` | ② |
| POST | `/items/:id/comments` | ② |
| GET | `/items/search?q=` | ② FTS5 全文 |
| GET/PUT | `/workflows` | ① 工作流定义读写 |
| GET/PUT | `/automations` | ★ 自动化规则读写 |
| GET | `/board` | ② 看板视图（保持 v0.1 兼容） |
| POST | `/ai/:action` | ★ clarify / split / prioritize / plan / report |
| GET | `/events` | — 事件流（后续可升级 WebSocket） |

**执行调度（v0.8 新增，模块⑥ / [[4-执行调度与AI]]）**：

| Method | Path | 说明 |
|--------|------|------|
| GET/POST | `/projects` | 注册 / 列出项目仓库（含 `watched` 巡检开关） |
| GET | `/runs` | 会话列表（按项目 / 工作项 / 状态筛选） |
| **POST** | `/items/:id/run` | ★ **开工**：起一次会话。body 带 phase / plan / backend / dry_run |
| GET | `/runs/:id` | 单次会话详情 + 里程碑时间线 |
| **GET** | `/runs/:id/stream` | ★ **SSE / WebSocket 实时事件流**（前端观察用，[[6-OpenClaw集成]] 待测） |
| POST | `/runs/:id/reply` | 回应 `awaiting_human`（人的决定写入 `HANDOFF.md`） |
| POST | `/runs/:id/abort` | 中止会话 |
| **GET** | `/runs/:id/takeover` | ★ 若有 `resumable_session_id`，返回 `claude --resume <id>`（+ cwd）；否则明确返回不可接管及人工处理指引 |
| GET | `/queue` | 当前队列与并发状态 |
| PUT | `/queue/config` | 改 `max_concurrent_runs`、repo 互斥策略（[[4-执行调度与AI]]） |
| POST | `/patrol/run` | 手动触发一次巡检（[[4-执行调度与AI]]） |
| GET | `/patrol/reports` | 巡检报告历史 |

> `POST /items/:id/run` 必须支持 `dry_run` —— 返回「将要发给 agent 的完整上下文包」而不真起会话。这是调试上下文组装（[[4-执行调度与AI]]）的唯一实用手段，也符合 [[4-执行调度与AI]] 的写操作预演原则。

---
