


项目级 Runs 视图：看到每张卡由哪个 Agent 执行、在哪个 worktree、是否等待人工。
并发与领取策略：同一 Card 单 Agent；同项目/同仓库设置并发上限；避免两个 Agent 改同一片代码。
交接与委派：一个 Card 可以明确拆出子 Card 并交给指定 Agent，但保持里程碑和交付事实不被执行结果自动篡改。
恢复与审计：Gateway 重启后，运行中的任务进入“恢复 / 异常 / 需人工处理”，而不是假装已完成。